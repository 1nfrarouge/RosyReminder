<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
  <nav>
    <a href="/">HOME</a>
    <% if (user) { %>
      <a href="/reminders/new">NEW REMINDER</a>
      <a href="/reminders">MY REMINDERS</a>
      <!-- User is logged in -->
      <a href="/logout">LOG OUT</a>
      <span>Welcome, <%= user.name %></span>
      <!-- Display user's avatar if available -->
      <% if (user.avatar) { %>
        <img src="<%= user.avatar %>" alt="User Avatar" style="height: 30px;">
      <% } %>
    <% } else { %>
      <!-- User is not logged in -->
      <a href="/auth/google" class="login">LOG IN <img src="https://i.imgur.com/FHjYyi0.png" style="height: 30px;"></a>
    <% } %>
  </nav>
</body>
</html>