<%- include('../partials/header') %>
<h2>MY REMINDERS</h2>

<% if (reminders.length) { %>
    <% reminders.forEach(function(reminder) { %>
        <article>
            <h4>Recipient <%= reminder.recipient %></h4>
        </article>
    <% }) %>
<% } else { %>
    <p>NO REMINDERS YET</p>
<% } %>

<%- include('../partials/footer') %>